import XCTest
@testable import ComposableComposition

final class ComposableCompositionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ComposableComposition().text, "Hello, World!")
    }
}
